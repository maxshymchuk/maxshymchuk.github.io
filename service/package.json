{
    "scripts": {
        "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/service.js",
        "serve": "npm run build && npm start",
        "start": "node dist/service.js"
    },
    "devDependencies": {
        "dotenv": "^16.4.5",
        "esbuild": "^0.21.4",
        "simple-git": "^3.24.0"
    },
    "dependencies": {
        "commander": "^12.1.0"
    }
}
